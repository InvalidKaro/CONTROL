/*
 * Previous minimal launcher diagnostic intentionally disabled.
 *
 * The normal ControlOS build uses src/main.cpp for setup()/loop().
 * Keeping this empty file prevents duplicate setup()/loop() symbols
 * if minimal_boot.cpp already exists in the repository.
 */
